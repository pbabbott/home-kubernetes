apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kubernetes Gateway API — CRDs only (Gateway, HTTPRoute, GatewayClass, …).
# Resource *instances* (e.g. shared listeners) stay in infra with the ingress deployment.
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.2.1/standard-install.yaml
