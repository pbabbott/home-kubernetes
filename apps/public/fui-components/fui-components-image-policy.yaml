apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fui-components
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: fui-components
    namespace: flux-system
  policy:
    semver:
      range: 0.x.x