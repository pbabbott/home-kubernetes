apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: ztunnel
  namespace: flux-system
spec:
  interval: 1h
  targetNamespace: istio-system
  dependsOn:
    - name: istio-cni
      namespace: flux-system
  chart:
    spec:
      chart: ztunnel
      version: "1.24.*"
      sourceRef:
        kind: HelmRepository
        name: istio
  values:
    profile: ambient
