apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: longhorn
  namespace: flux-system
spec:
  values:
    defaultBackupStore:
      backupTarget: nfs://192.168.4.124:/volume1/Backups/longhorn-nonprod
      backupTargetCredentialSecret: cifs-secret
