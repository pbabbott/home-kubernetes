apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/external-dns

patches:
  - target:
      kind: HelmRelease
      name: external-dns
    patch: |-
      - op: add
        path: /spec/values/txtOwnerId
        value: prod-gen2
      - op: add
        path: /spec/values/domainFilters
        value: ["abbottland.io"]
