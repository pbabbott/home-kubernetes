---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kube-prometheus-stack
  namespace: flux-system
spec:
  values:
    prometheus:
      prometheusSpec:
        externalLabels:
          cluster: prod-gen2
