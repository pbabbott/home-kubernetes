apiVersion: v1
kind: Service
metadata:
  name: gluetun-sync
  namespace: media
  labels:
    api: gluetun
    app: gluetun-sync
spec:
  ports:
    - name: http
      port: 4000
      protocol: TCP
  selector:
    api: gluetun
