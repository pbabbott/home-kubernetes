apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: gluetun-sync
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: gluetun-sync
  filterTags:
    pattern: '^\d{8}-\d+-[a-f0-9]{7}$'
  policy:
    alphabetical:
      order: asc
