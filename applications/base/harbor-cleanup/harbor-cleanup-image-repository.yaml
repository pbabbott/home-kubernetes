apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: harbor-cleanup
  namespace: flux-system
spec:
  image: harbor.local.abbottland.io/library/harbor-cleanup
  interval: 5m
  secretRef:
    name: regcred-flux
