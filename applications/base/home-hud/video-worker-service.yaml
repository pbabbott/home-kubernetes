apiVersion: v1
kind: Service
metadata:
  name: video-worker
  namespace: home-hud
  labels:
    app: video-worker
spec:
  selector:
    app: video-worker
  ports:
    - name: http
      port: 4003
      targetPort: 4003
  type: ClusterIP
