apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: fui-components
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: fui-components
  policy:
    semver:
      range: ">=0.0.0"
