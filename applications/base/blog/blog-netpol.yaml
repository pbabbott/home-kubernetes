apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: blog
  namespace: blog
spec:
  podSelector:
    matchLabels:
      app: blog
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: istio-system
      ports:
        - port: 3000
  egress: []
