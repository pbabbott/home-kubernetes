apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: nas
  namespace: asustor
  labels:
    pihole-dns-enabled: "true"
spec:
  endpoints:
    - dnsName: nas.local.abbottland.io
      recordType: A
      targets:
        - 192.168.4.124
